org.jetbrains.dokka.experimental.gradle.pluginMode=V2EnabledWithHelpers
# Dokka's embedded Kotlin analyzer doesn't support JDK 25 yet.
# Docs generation (dokkaGeneratePublicationHtml) requires JDK <= 21.
# In CI this is handled by actions/setup-java (JDK 21). Locally, set JAVA_HOME to JDK 21.
