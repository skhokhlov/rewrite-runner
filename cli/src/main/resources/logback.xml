<configuration>
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>[%-5level] %message%n</pattern>
        </encoder>
    </appender>

    <!-- Show io.github.skhokhlov.rewriterunner logs at INFO and above on stderr -->
    <logger name="io.github.skhokhlov.rewriterunner" level="INFO" additivity="false">
        <appender-ref ref="STDERR"/>
    </logger>

    <!-- Suppress all other library noise (OpenRewrite, Maven Resolver, etc.) -->
    <root level="OFF"/>
</configuration>
